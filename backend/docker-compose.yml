version: '3.3'

services:
  db:
    image: postgres:10.1-alpine
  backend:
    build: .
    image: split/backend:latest
    volumes:
      - ./src:/app/src
    ports:
      - "8000:80"
    depends_on:
      - db

